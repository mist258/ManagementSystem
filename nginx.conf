server {
    listen 80;
    server_name localhost;

    location /api/ {
        proxy_pass http://app:8002/;
        proxy_set_header Host $host;
    }

     location / {
        proxy_pass http://app:8002;
        proxy_set_header Host $host;
    }
}